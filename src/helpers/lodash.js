const get = require('lodash/get');
const uniq = require('lodash/uniq');
const findIndex = require('lodash/findIndex');
const sortBy = require('lodash/sortBy');

module.exports = { get, uniq, findIndex, sortBy };
